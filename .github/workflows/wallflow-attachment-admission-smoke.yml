name: Wallflow attachment admission smoke

on:
  push:
    paths:
      - wallflow-attachment-admission-smoke/**
      - .github/workflows/wallflow-attachment-admission-smoke.yml
  workflow_dispatch:

jobs:
  smoke:
    runs-on: macos-15
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Show Swift toolchain
        run: swift --version
      - name: Build with complete concurrency checking
        run: >-
          swift build
          --package-path wallflow-attachment-admission-smoke
          -Xswiftc -strict-concurrency=complete
          -Xswiftc -warnings-as-errors
      - name: Validate immutable attachment ownership
        run: >-
          swift run
          --package-path wallflow-attachment-admission-smoke
          wallflow-attachment-admission-smoke
